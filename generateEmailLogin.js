  function createEmail() {
    var firstName = "";
    var lastName = "";
    var emailDomain = ""
    var email = firstName.toLowerCase() + "." + lastName.toLowerCase() + emailDomain;
  }

  function createLogin() {
    var firstName = "";
    var lastName = "";
    var login = firstName.toLowerCase().charAt(0) + lastName.toLowerCase();
  }
